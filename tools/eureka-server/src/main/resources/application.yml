server:
  port: 8761
spring:
  application:
    name: eureka-server
eureka:
  server:
    enable-self-preservation: false
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: false
    fetch-registry: false
